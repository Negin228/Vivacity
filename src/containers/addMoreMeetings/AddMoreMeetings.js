import React from 'react';
import { Button } from '../../components';

const AddMoreMeetings = ({ listing }) => {
  console.log(listing);
  return <Button>Add More Meetings</Button>;
};

export default AddMoreMeetings;
